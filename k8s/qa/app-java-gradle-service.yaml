apiVersion: v1
kind: Service
metadata:
  name: app-java-gradle-service
  namespace: qa
spec:
  type: NodePort
  selector:
    app: app-java-gradle
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30082
